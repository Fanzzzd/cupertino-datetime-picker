# Changesets

Every change that reaches npm gets a file here: `pnpm changeset`, pick the
bump, describe the change from a user's side. The release workflow turns the
pending files into a "Version Packages" PR; merging it publishes.
